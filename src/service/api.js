import {categoryList,addCategoryName,deleteCategory} from './category/api_category'
import {configList,addConfig} from './config/api_config'

//默认导出，需要模块名字，来做引用
export default {
    categoryList,
    addCategoryName,
    deleteCategory,
    configList,
    addConfig
}
